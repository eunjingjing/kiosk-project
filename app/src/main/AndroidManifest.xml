<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/> <!--인터넷 권한 선언-->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JAVA_PROJECT"
        tools:targetApi="34">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Regist">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".MainPage">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".OrderPage">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".OrderPage2">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".UserPage">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".MainAdmin">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".MonthlySales">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".DailySales">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
        <activity android:name=".tensor">
            <!-- 이 부분에 인텐트 필터 및 설정을 추가할 수 있습니다. -->
        </activity>
    </application>

</manifest>